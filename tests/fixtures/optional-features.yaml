# yaml-language-server: $schema=../../winix-cfg.schema.json

version: 1
system:
  windows:
    features:
      optional:
        App.WirelessDisplay.Connect:
          provider: capability
          state: enabled
