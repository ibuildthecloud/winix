{
  "protocol_version": 2,
  "name": "nerd-fonts",
  "path": "fonts.nerd_fonts",
  "placements": ["system", "user"],
  "entrypoint": "plugin.ps1",
  "schema": "schema.json",
  "requires": {
    "powershell": ">=7.0",
    "administrator": false,
    "commands": ["Invoke-RestMethod", "Invoke-WebRequest", "Expand-Archive"]
  }
}
